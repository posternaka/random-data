import { generateAddress } from '../../generate/generateAddressEn';
import { generatePhoneEn as generatePhone } from '../../generate/generatePhone';
import { generateId } from '../../generate/generateId';
import { generateFullNameGeneral  as generateFullName } from '../../generate/generateFullName';

export default { generateFullName, generateId, generateAddress, generatePhone };