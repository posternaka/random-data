import { generateAddress } from '../../generate/generateAddressRu';
import { generatePhoneRu as generatePhone } from '../../generate/generatePhone';
import { generateId } from '../../generate/generateId';
import { generateFullNameSpecial  as generateFullName } from '../../generate/generateFullName';

export default { generateFullName, generateId, generateAddress, generatePhone };